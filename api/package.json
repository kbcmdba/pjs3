{
  "name": "pjs3-api",
  "version": "0.0.0",
  "private": true,
  "description": "PJS3 backend — Fastify API with MySQL via Drizzle and auth via Better-Auth",
  "license": "GPL-2.0-only",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "fastify": "^5",
    "mysql2": "^3.22.2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^3"
  }
}
